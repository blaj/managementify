<tr>
  <td>
    {{ loop.index }}
  </td>

  <td>
    {{ userPaginatedListItemDto.username }}
  </td>

  <td>
    {{ userPaginatedListItemDto.email }}
  </td>

  <td>
    <div class="btn-group">
      <button
          type="button"
          class="btn btn-primary btn-sm dropdown-toggle"
          data-mdb-dropdown-init data-mdb-ripple-init aria-expanded="false">
        {{ 'actions'|trans }}
      </button>

      <ul class="dropdown-menu">
        <li>
          <a class="dropdown-item" href="{{ path('user_details', { id: userPaginatedListItemDto.id }) }}">
            {{ 'view'|trans }}
          </a>
        </li>

        <li>
          <a class="dropdown-item" href="{{ path('user_update', { id: userPaginatedListItemDto.id }) }}">
            {{ 'edit'|trans }}
          </a>
        </li>

        <li>
          <form action="{{ path('user_delete', { id: userPaginatedListItemDto.id }) }}" method="post">
            <button type="submit" class="dropdown-item">
              {{ 'delete'|trans }}
            </button>

            <input type="hidden" name="_method" value="DELETE">
          </form>
        </li>
      </ul>
    </div>
  </td>
</tr>