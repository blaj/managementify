{% extends 'authorized-base.html.twig' %}

{% block content %}
  <div class="card">
    <div class="card-header py-3 d-flex justify-content-between align-items-center">
      <h5 class="mb-0">
        <strong>
          {{ 'clients'|trans }}
        </strong>
      </h5>

          <a class="btn btn-primary btn-sm" data-mdb-ripple-init href="{{ path('client_create') }}" role="button">
            <i class="fa fa-plus me-2"></i>

            {{ 'add-client'|trans }}
          </a>
    </div>

    <div class="card-body">
      {% include 'client/list/partial/_filter.html.twig' %}

      {% include 'client/list/partial/_table.html.twig' %}

      {% include 'client/list/partial/_pagination.html.twig' %}
    </div>
  </div>
{% endblock %}